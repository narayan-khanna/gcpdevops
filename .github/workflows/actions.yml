name: first workflow
on: workflow_dispatch
jobs:
  first-job: 
    runs-on: ubuntu-latest
    steps:
      - name: Print greetings
        run: echo "Hello Github Actions"
      - name: Print goodBye
        run: echo "Done-bye"
